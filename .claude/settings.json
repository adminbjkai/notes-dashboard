{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'FILE=\"$CLAUDE_FILE_PATH\"; if [[ \"$FILE\" == */backend/*.py ]]; then echo \"🔍 Running backend quality checks...\"; cd /Users/m17/codex-bjkai/notes-dashboard-by-claude && docker compose exec -T backend ruff check app/ --output-format=concise 2>&1 | head -20 || true; docker compose exec -T backend mypy app --ignore-missing-imports --no-error-summary 2>&1 | grep -E \"error:|warning:\" | head -10 || echo \"✅ mypy: OK\"; fi'"
          },
          {
            "type": "command",
            "command": "bash -c 'FILE=\"$CLAUDE_FILE_PATH\"; if [[ \"$FILE\" == */frontend/*.ts || \"$FILE\" == */frontend/*.tsx ]]; then echo \"🔍 Running frontend quality checks...\"; cd /Users/m17/codex-bjkai/notes-dashboard-by-claude/frontend && npm run lint --silent 2>&1 | head -20 || true; npm run type-check --silent 2>&1 | grep -E \"error|Error\" | head -10 || echo \"✅ type-check: OK\"; fi'"
          }
        ]
      }
    ]
  }
}
