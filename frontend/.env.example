# Frontend Environment Variables
# Copy to .env.local for local development outside Docker

# Server-side API URL (Next.js Server Components)
# - Docker: http://backend:8000 (set in docker-compose.yml)
# - Non-Docker: http://localhost:8000
API_URL=http://localhost:8000

# Client-side API URL (browser requests)
# - Always: http://localhost:8000 (browser accesses host machine)
NEXT_PUBLIC_API_URL=http://localhost:8000
